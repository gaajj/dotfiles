# pi over cloudflared tunnel
Host pi
  HostName localhost
  User gaaj
  ProxyCommand cloudflared access ssh --hostname pi.gaaj.dev
  SetEnv TERM=xterm-256color

# pi over local network
Host lpi
  HostName 192.168.2.4
  Port 8999
  User gaaj
  SetEnv TERM=xterm-256color
